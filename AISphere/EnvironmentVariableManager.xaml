<Window x:Class="AISphere.EnvironmentVariableManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Environment Variable Manager" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <ComboBox x:Name="VariableNameComboBox" Width="200" Margin="0,0,10,0">
                <ComboBoxItem Content="OPENAI_API_KEY"/>
            </ComboBox>
            <Grid Width="200" Margin="0,0,10,0">
                <TextBox x:Name="VariableValue"/>
                <TextBlock Text="Variable Value" IsHitTestVisible="False" Foreground="Gray" VerticalAlignment="Center" Margin="5,0,0,0" Visibility="{Binding Text, ElementName=VariableValue, Converter={StaticResource StringToVisibilityConverter}}"/>
            </Grid>
            <Button Content="Set" Click="SetButton_Click" Margin="0,0,10,0"/>
            <Button Content="Delete" Click="DeleteButton_Click"/>
        </StackPanel>

        <ListBox x:Name="VariableList" Grid.Row="1" Margin="10"/>

        <Button Content="Refresh" Click="RefreshButton_Click" Grid.Row="2" HorizontalAlignment="Right" Margin="10"/>
    </Grid>
</Window>
