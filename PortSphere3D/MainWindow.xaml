<Window x:Class="PortSphere3D.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortSphere3D"
        mc:Ignorable="d"
        Title="PortSphere3D" Height="800" Width="1200" MinHeight="800" MinWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Margin="10" Grid.Row="0" VerticalAlignment="Top">
            <TextBlock Text="Texture:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="TextureComboBox" Width="180" SelectionChanged="TextureComboBox_SelectionChanged">
                <ComboBoxItem Content="Grid" IsSelected="True"/>
                <ComboBoxItem Content="Octagon Grid"/>
                <ComboBoxItem Content="Stars"/>
            </ComboBox>
            <TextBlock Text="  Sphere Size:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <Slider x:Name="SphereSizeSlider" Width="200" Minimum="1" Maximum="6" Value="2.1" TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="SphereSizeSlider_ValueChanged"/>
            <TextBlock Text="  Spin Speed:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <Slider x:Name="SpinSpeedSlider" Width="160" Minimum="0" Maximum="2" Value="0.3" TickFrequency="0.05" IsSnapToTickEnabled="True" ValueChanged="SpinSpeedSlider_ValueChanged"/>
            <TextBlock Text="  Sphere Color:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <ComboBox x:Name="SphereColorComboBox" Width="140" SelectionChanged="SphereColorComboBox_SelectionChanged">
                <ComboBoxItem Content="Green" IsSelected="True"/>
                <ComboBoxItem Content="Blue"/>
                <ComboBoxItem Content="Red"/>
                <ComboBoxItem Content="Purple"/>
                <ComboBoxItem Content="Gray"/>
                <ComboBoxItem Content="White"/>
            </ComboBox>
            <TextBlock Text="  Direction:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <Grid x:Name="SpinDial" Width="64" Height="64" MouseLeftButtonDown="SpinDial_MouseLeftButtonDown" MouseMove="SpinDial_MouseMove" MouseLeftButtonUp="SpinDial_MouseLeftButtonUp" ToolTip="Drag to set spin direction">
                <Ellipse Width="64" Height="64" Fill="#222" Stroke="#555" StrokeThickness="2"/>
                <Polygon x:Name="SpinDialPointer" Points="32,10 26,24 38,24" Fill="#ddd" RenderTransformOrigin="0.5,0.5">
                    <Polygon.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Polygon.RenderTransform>
                </Polygon>
            </Grid>
        </WrapPanel>
        <Grid Grid.Row="1">
            <Viewport3D x:Name="MainViewport" ClipToBounds="True"/>
            <Canvas x:Name="LightHandlesCanvas" Background="Transparent" IsHitTestVisible="True"/>
        </Grid>
    </Grid>
</Window>
